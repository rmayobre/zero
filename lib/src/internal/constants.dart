import 'package:meta/meta.dart';

@internal
const conditionCheckMsg = 'Failed to meet condition';

@internal
const nullCheckMsg = 'Value is null';

@internal
const typeCheckMsg = 'Value is not type of';