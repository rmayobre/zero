name: zero-nads
description: A zero runtime cost framework for structuring code within monad-style wrappers.
version: 0.0.1
repository: https://github.com/rmayobre/zero-nads

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - monad
  - abstraction
  - extensions